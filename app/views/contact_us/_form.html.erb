<%= form_with(model: contact_u, local: true) do |form| %>
  <% if contact_u.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact_u.errors.count, "error") %> prohibited this contact_u from being saved:</h2>

      <ul>
      <% contact_u.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :telefone %>
    <%= form.text_field :telefone %>
  </div>
  
  <div class="field">
    <%= form.label :motivo %>
    <%= form.select :motivo, ['Reclamação', 'Dúvida', 'Serviço CleanCar'], include_blank: ''  %>
  </div>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div class="actions">
    <%= form.submit "Enviar"%>
  </div>
<% end %>
